# Cycle27: combine the verified queen-aware evaluator with check extensions

The cycle26 engine still chose56...Bxe8 at depth7 and valued its drawing
promotion continuation about+366cp. The earlier cycle14 trace identified an
overvalued passed pawn against a queen. Cycle15's bounded correction repaired
that target, but candidate49 failed its later practical comparison; preserve
that entire record and do not repeat those games or promote49 automatically.

Make a new one-function composition: keep frozen26's two check-extension
credits and exact pawn-legality shortcut, replacing only classical with the
identical function from candidate49. At phase<=8, a side without a queen
facing an enemy queen gets passed-pawn endgame multiplier2 instead of5. Keep
the coefficient/scope fixed and every other evaluation term, search function,
weight, clock and policy unchanged. No failed25 ordering is included.

Retain the six old queen-aware scope/symmetry/state tests by XML hash, exact
donor function and constant parity. Run six new integration checks: three
material-context accumulator-dispatch cases must match the verified donor;
mate/draw precedence in two cases; one interrupted real-position search must
restore board, history and accumulator. Compilation precedes finite search
limits. Preserve all failures; do not rerun unchanged passed tests.

Use selected51 versus the composition on all17 one-second clock roots and the
three250k-node/8second diagnostics, policy disabled equally, full actual histories
and cleared state. Require init<=90s, legal/restored/depth>=1, outer clock<=1.25s
and node<=8.25s; each300second child bound terminates only its owned Windows
process tree. Require2048MiB free disk/768MiB RAM before each heavy process,
max20minute capacity wait, serial hidden Normal-priority execution, STOP flags.

Keep the original26 rule and add a requirement for this new motivating target:
fewer original CLOCK mistakes, king avoidsKf1 in at least one mode, and the
clock endgame avoidsBxe8. Only then review20choices/build at80k/320k using
compatible existing references/cache, maximum16M new teacher nodes before cache.
Require lower clock mean regret at BOTH budgets, no newly stable paired200cp
error or mate loss in either mode, king<=50cp at both budgets in one mode, and
the endgame clock choice<=50cp at both budgets. The already losing rook receives
no exemption. Do not weaken or retune the gate after results.

Only fullPASS permits strict read-only packaging and exactly two120+0.5 games
against51 from unused prepared opening index6 in both colours, requiring at
least50%score and no candidate failure before further selection. No automatic
promotion, rating estimate or long consistency study. Failed gate retains51.

Engine evaluation correctness is first. For learning, a less biased classical
base could make future residual targets more reachable, but that is a rationale,
not a newly fitted network or proof of improved learning. Existing opponent
continuations,56Bxe8/Rb7 traces and all17 audited roots supply the targeted data;
no broad GM download, blind epochs or residual-cap increase is needed here.
