# Quiet-check test collection repair

The first cycle21 attempt stopped during test collection, before compilation,
position probes, teacher work or games. The test imported arrays from the
project root engine.compiled_driver, but that module exists in the candidate
packages rather than the older root engine package. The ImportError and original
tests, controller, configuration and preparation manifest remain preserved.

The corrected test imports the existing arrays utility from
experiments.aspiration_driver. Its arrays function is AST-identical to the
selected51 utility; merely importing that module does not run its rejected
aspiration search. All test cases, candidate core bytes, targets, node/clock
budgets and acceptance criteria remain unchanged.

Use separate tests/test_quiet_checks_runtime.py, recovered gate script,
configuration and cycle-21-recovered output directory. Keep cycle-21 untouched.
This repairs an actual collection error; no completed test, position measurement,
training epoch or failed strength gate is repeated. No playing code from the
aspiration prototype is mixed into the candidate.
