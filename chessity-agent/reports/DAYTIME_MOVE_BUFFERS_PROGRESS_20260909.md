# Four completed wins; rated pairs still running

At14:03BST the unselected buffered-search candidate had beaten exactv1.55 by2-0
and exactv1.53 by2-0, winning with both colours in each comparison. All four games
ended by checkmate without operational failures. These are small development
comparisons, not a calibrated Elo estimate or a guarantee of broader superiority.

All243 candidate moves were reviewed:149 positive labels and18 negative labels,
including11 middlegame and7 endgame errors. The wins therefore still supply
useful correction targets. Examples include recapturing with ...Rxf6 instead of
...gxf6, Qc6 instead of Qc5, and ...g4 instead of ...Rc2. Every alternative remains
tied to its saved game, history and independent teacher labels. Uncertain targets
are retained as unknown; no root score is used as a descendant value label.

The nominal2400 pair is in progress, with2600 next and conditional2800 afterward.
Selectedv1.55 remains unchanged until the complete screen and release audit pass.
The feedback-policy checkpoints remain experimental and playing weights frozen.
Progress snapshot move-buffers-progress-01 contains only the two completed pairs.
