# Isolate the reference JIT without changing the experiment

Trial 01 stopped at its tests. Nine tests passed, including all foreign-history
score guards and the demonstration that foreign hints are visited first. Four
full-width comparisons failed while compiling the preserved parent loaded through
a dynamic module alias: Numba reported an unresolved recursive symbol. The
performance benchmark and teacher-quality stages did not run.

Preserve those logs, preparation, prototype and tests. Trial 02 keeps exactly the
same candidate core, nineteen roots, thirteen assertions and all predeclared
thresholds. Run the parent reference in a separate process through its normal
agent package and startup path. Save the four bounded reference scores with its
manifest, then close that process before prototype tests begin. The tests compare
against the saved reference; they do not load two recursive search engines into
one process. A shared non-engine call helper keeps the comparison inputs identical.

The warmed performance worker already loads each actual agent in its own process.
This correction repairs only reference setup. It does not relax an algorithmic
failure, improve a chess result or authorise promotion. Preserve trial 02 if it
also fails, and keep all current serial-work, capacity, STOP and deadline guards.
