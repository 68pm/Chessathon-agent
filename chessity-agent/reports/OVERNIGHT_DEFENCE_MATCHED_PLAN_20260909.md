# Isolate defence with the same compiler repair on both sides

Declared before the fresh comparison. Defence-coalesced-01initialized its prototype
in64.412s with one compiled search variant, but the original53control exceeded90s.
No position probes ran. The unchanged old control has failed startup repeatedly;
do not keep using that brittle control setup or call its failures chess victories.

This new experimental design copies two frozen builds without changing either:
the coalesced-search-01prototype is the control; defence-coalesced-01prototype is
the defence candidate. Both have the same complete compiler repair. The control's
search logic/weights are53's and its fixed-work score/move/depth/node equivalence
already passed on all22roots. Its failed timed-quality gate against the exact53
ZIP remains recorded; it is a matched control here, not a promoted champion.

Freeze exact copied manifests and reused test provenance. Reuse the10passed
defence integration checks and15passed original compiled threat checks because
the tested runtime bytes are unchanged. Run fresh prototype/control90s starts,
88ABBA1s choices on the same22development roots and independent80k/320k review
after workers close. Keep the original defence quality gate: a>=100cp repair at
both budgets, nonincreasing mean regret at both, no added paired200cp/mate-loss
errors and legal/restored/on-time choices. Do not count any original failed run
as a measured defence failure/success or retry its original pairing unchanged.

If passed, strict read-only validation and four120s+0.5s feedback_matches games
will compare the defence candidate with this SAME compiler-repaired53control,
not mislabel it as the exactv1.53ZIP. Require>=50%points, one clean win and no
operational failures before a provisional release can be considered. Conditional
nominal pairs then start at2400 and advance after clean wins. Report all evidence,
including contrary rated outcomes, before selecting any release; no automatic
upload or calibrated Elo claim follows this control substitution.

Weights remain frozen; known value-fit opening/endgame regressions remain a
separate learning target. Existing verified field mistakes suffice for this test.
