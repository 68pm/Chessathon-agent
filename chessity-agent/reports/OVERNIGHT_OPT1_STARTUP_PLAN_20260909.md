# Reduce compiler startup cost with measured runtime checks

The countercheck candidate scored2W1D1L against the compiler-repaired53 control,
then0W0D2L at nominal2400: one played checkmate loss and one90-second startup
failure. The failure's recorded available RAM was2118MB before and2133MB after;
no out-of-memory exception was recorded. Preserve every result and retain53.

Test one process-local compiler configuration change: set NUMBA_OPT=1 before
importing Numba through the agent. The installed compiler's own configuration
source explicitly supports levels0/1/2/3/max and defaults to3. No machine-wide
setting, search algorithm, model parameter, opening policy or inference
permission changes. The prior candidate remains frozen and is not retried.

Re-run its24 numerical, state-restoration and countercheck integration checks
under the changed compiler level, using byte-identical kernel/helper sources.
Require prototype startup<=65seconds, leaving headroom below the unchanged90s
limit. The matched classical control still uses the default level3.

Before timed comparison, reproduce each of the26 saved prototype decisions at
its previously completed depth, with fresh cleared tables, at most12seconds
per probe. Require exact move, score and completed depth. Those are fixed-depth
checks against frozen parent evidence, not new full-game or fixed-node parity.
Record all failures and stop without implicit timing retries.

Then run the same104 ABBA one-second probes against the compiler-repaired53
control. Require nondecreasing mean completed depth, nonincreasing regret at
both80k/320k teacher budgets, no increased major/mate-error counts and a100cp
repair. A pass permits strict read-only validation and a new short comparison,
followed by conditional clean-win rated pairs. No automatic promotion or Elo.

Future short-screen timeouts may be bounded at45minutes for four comparison
games and25minutes per rated pair, based on the completed parent's30-minute
comparison and10-minute rated stage. This changes only maximum job duration;
game clocks remain120s+0.5s, all outcomes are reviewed, and the06:40 heavy-work
cutoff still reserves the whole stage. Never discard a slow or failed result.
