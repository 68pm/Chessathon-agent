# Windows source-hash path correction

archive-values-01 stopped before engine startup because the reused label runner
hashed long review filenames with ordinary Windows paths. No teacher nodes or
labels were produced. Preserve its source, preparation and failure record.

archive-values-02 uses the unchanged endpoint protocol and targets, with the
existing extended-path adapter only for source-file hashing. Do not rerun the
failed preparation. The same23.2M-node cap, no-fit scope, exposed-development
attribution and06:40BST cutoff apply.
