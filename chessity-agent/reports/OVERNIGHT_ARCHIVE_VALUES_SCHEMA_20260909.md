# Empty target placeholder adapter

The Windows-path correction reached the first endpoint analysis, then the older
label runner rejected a duplicate target_stm_cp keyword. The newer planner stores
that field as an explicit null placeholder; the older runner adds the measured
field itself. Preserve archive-values-02 and its cached teacher analyses. Its
state did not record the requested-node count; at most400k nodes were requested
for that first endpoint.

archive-values-03 asserts that all58 placeholders are null and that no other
runner output key collides, then removes only that empty placeholder in the new
derived preparation. Each value still comes from independent endpoint analysis.
Targets, groups, budgets, acceptance rules, deadline and no-fit scope are unchanged.
