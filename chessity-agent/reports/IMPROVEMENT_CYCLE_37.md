# Cycle37: explicit diagnostic call for the v1.53 defensive cases

Cycle36 initialized v1.53 in73.704seconds but its first diagnostic call failed
the combined node/wall-time assertion. No branch, endpoint or teacher result was
saved. The log does not distinguish the two limits, so the exact cause is not
claimed as established. Preserve all original36 sources, plans and failure logs.

The wrapper had omitted v1.53's two new optional search arguments. The successful
cycle35 proof called them explicitly. This new diagnostic supplies quiet-check
credits4 and attacker0, matching the production recursive signature, and verifies
statically that every search argument is present. It records wall time, node
count, interruption, restoration and newly compiled signatures before checking
any measurement bound. Any new specialization during a measured probe invalidates
it and stops the attempt. This is a corrected call boundary with new observability,
not an unchanged timing retry or relaxed gate. No external function warmup or
extra time is hidden outside the original90-second initialization limit.

The original three roots and six choices remain byte-identical to36:10...Nc6/Kh7,
14.Qb3/h3 and19...Bh4/Be6. Each full-window depth6 probe stays500k nodes/8seconds,
plus at most30 local endpoint quiescence probes at100k/2seconds. Bounds permit
0.25seconds wall overhead. Null all interrupted or invalid scores. Trace exact
entries only, including the quiet-check and remaining-extension TT contexts.
Student endpoints and both teacher budgets' best/played prefixes4/8 remain the
counterfactual coverage. After student exit only, independently review endpoints
at80k/320k, maximum12million requested teacher nodes; student maximum6million.
Worker process trees remain bounded360seconds. No extra game, model change or fit.

Record legal/restored histories and descriptive king shelter, absolute pins,
material, pressure and passed pawns. Engine diagnosis comes first; useful learning
requires independently verified descendant targets and compatible output range
second; targeted data comes from these actual losses third. Do not infer the
cause from a feature or fit root labels onto endpoints. Preserve the selected53
ZIP and all earlier failed attempts. No broad data collection or blind epochs.

Use hidden Normal priority4, serial local work, STOP flags and2048MiB disk plus
768MiB RAM before each heavy process. Capacity waits remain at most20minutes.
